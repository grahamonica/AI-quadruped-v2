name: default

simulator:
  mujoco:
    noslip_iterations: 6

spawn_policy:
  strategy: origin

friction:
  foot_kinetic: 0.45

quality_gates:
  performance_budget_seconds: 12.0
  performance_eval_runs: 2
